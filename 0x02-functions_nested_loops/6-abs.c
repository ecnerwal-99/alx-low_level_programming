#include <stdio.h>
#include "main.h"
/**
 * _abs - check the code
 * @m:  is the type of patameter int
 * Description: absolute
 * Return: 0
 */
int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (m * -1);
}
