#include <stdio.h>
#include "main.h"
/**
 * _abs - check the code
 * int is the type of patameter
 * Description: absolute
 * Return: 0
 */
int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (m * -1);
}
